import {ReactNode} from 'react';import {NavLink,Link} from 'react-router-dom';import {Home,Search,Heart,MessageCircle,Users,User,Settings,LogOut,Bell} from 'lucide-react';import {useAuth} from '../contexts/AuthContext';
const links=[['/','Home',Home],['/discover','Discover',Search],['/matches','Matches',Heart],['/messages','Messages',MessageCircle],['/communities','Communities',Users],['/profile','Profile',User]] as const;
export default function Layout({children}:{children:ReactNode}){const{signOut}=useAuth();return <div className="shell"><header className="topbar"><Link to="/" className="brand"><span className="mark">✦</span>ADUSTECH <b>Connect</b></Link><nav className="desktop-nav">{links.map(([to,label,Icon])=><NavLink key={to} to={to} end={to==='/'} className={({isActive})=>isActive?'active':''}><Icon size={17}/>{label}</NavLink>)}<NavLink to="/notifications"><Bell size={17}/>Notifications</NavLink><NavLink to="/settings"><Settings size={17}/>Settings</NavLink><button className="ghost-button" onClick={signOut} aria-label="Log out"><LogOut size={17}/></button></nav></header><main>{children}</main><nav className="mobile-nav">{links.filter(x=>x[0]!=='/communities').map(([to,label,Icon])=><NavLink key={to} to={to} end={to==='/'} className={({isActive})=>isActive?'active':''}><Icon size={19}/><small>{label}</small></NavLink>)}</nav></div>}
