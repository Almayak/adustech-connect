import {useEffect,useMemo,useState} from 'react';
import {useAuth} from '../contexts/AuthContext';
import {supabase} from '../lib/supabase';

type Student={id:string;full_name:string;username:string;bio:string|null;level:string|null;faculty:{name:string}|null};
export default function DiscoverPage(){const{session}=useAuth();const[students,setStudents]=useState<Student[]>([]);const[query,setQuery]=useState('');const[faculty,setFaculty]=useState('');const[status,setStatus]=useState('');useEffect(()=>{if(!supabase||!session)return;supabase.from('profiles').select('id,full_name,username,bio,level,faculty:faculties(name)').neq('id',session.user.id).eq('is_visible',true).order('full_name').limit(60).then(({data,error})=>{if(error)setStatus(error.message);else setStudents((data??[]) as unknown as Student[])})},[session]);const faculties=useMemo(()=>Array.from(new Set(students.map(s=>s.faculty?.name).filter(Boolean) as string[])).sort(),[students]);const filtered=students.filter(s=>{const text=`${s.full_name} ${s.username} ${s.bio??''} ${s.faculty?.name??''}`.toLowerCase();return(!query||text.includes(query.toLowerCase()))&&(!faculty||s.faculty?.name===faculty)});async function connect(id:string){if(!supabase||!session)return;setStatus('Sending connection request…');const{error}=await supabase.from('connections').insert({requester_id:session.user.id,recipient_id:id});setStatus(error?(error.code==='23505'?'Connection request already exists.':error.message):'Connection request sent.')}return <section className="page"><div className="eyebrow">ADUSTECH Connect</div><h1>Discover students</h1><p>Find study partners and meaningful connections across the verified ADUSTECH faculty list.</p><div className="discover-controls"><input aria-label="Search students" placeholder="Search by name, username or interest" value={query} onChange={e=>setQuery(e.target.value)}/><select aria-label="Filter by faculty" value={faculty} onChange={e=>setFaculty(e.target.value)}><option value="">All available faculties</option>{faculties.map(name=><option key={name}>{name}</option>)}</select></div>{status&&<p className="form-status">{status}</p>}<div className="student-grid">{filtered.map(student=><article className="student-card" key={student.id}><div className="avatar">{student.full_name.slice(0,1).toUpperCase()}</div><h2>{student.full_name}</h2><p className="muted">@{student.username}{student.level&&` · ${student.level}`}</p>{student.faculty&&<p>{student.faculty.name}</p>}<p>{student.bio||'This student has not added a bio yet.'}</p><button className="primary" onClick={()=>connect(student.id)}>Connect</button></article>)}</div>{!filtered.length&&!status&&<div className="empty-state"><h2>No students found</h2><p>Try another search, or complete your profile so other students can discover you.</p></div>}</section>}
