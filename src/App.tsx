import {BrowserRouter,Link,Route,Routes} from 'react-router-dom';import {AuthProvider} from './contexts/AuthContext';import {supabase} from './lib/supabase';
function Home(){return <main className="hero"><div className="brand">✦ ADUSTECH <b>Connect</b></div><p className="eyebrow">Student-first · Respectful · Private by design</p><h1>Meet. Connect.<br/><span>Match. Belong.</span></h1><p>Discover friends, study partners, communities and meaningful connections across ADUSTECH.</p><div className="actions"><Link className="primary" to="/signup">Start connecting</Link><Link className="secondary" to="/discover">Explore students</Link></div><small>ADUSTECH Connect is an independent student platform and is not affiliated with or officially endorsed by ADUSTECH unless otherwise stated.</small></main>}
function Auth({signup=false}:{signup?:boolean}){const submit=async(e:React.FormEvent<HTMLFormElement>)=>{e.preventDefault();if(!supabase)return;const f=new FormData(e.currentTarget);const email=String(f.get('email'));const password=String(f.get('password'));const result=signup?await supabase.auth.signUp({email,password,options:{data:{full_name:String(f.get('name')||'')}}}):await supabase.auth.signInWithPassword({email,password});if(result.error)alert(result.error.message)};return <main className="auth"><form onSubmit={submit}><Link to="/" className="brand">✦ ADUSTECH <b>Connect</b></Link><h1>{signup?'Create your account':'Welcome back'}</h1>{signup&&<input name="name" placeholder="Full name" required/>}<input name="email" type="email" placeholder="Email" required/><input name="password" type="password" placeholder="Password (8+ characters)" minLength={8} required/><button className="primary">{signup?'Sign up':'Log in'}</button><Link to={signup?'/login':'/signup'}>{signup?'Already registered? Log in':'New here? Create an account'}</Link></form></main>}
function Discover(){return <main className="page"><h1>Discover students</h1><p>No students to show yet. Connect Supabase and create your profile to appear here.</p><Link className="secondary" to="/">Back home</Link></main>}
export default function App(){return <AuthProvider><BrowserRouter><Routes><Route path="/" element={<Home/>}/><Route path="/login" element={<Auth/>}/><Route path="/signup" element={<Auth signup/>}/><Route path="/discover" element={<Discover/>}/><Route path="*" element={<Discover/>}/></Routes></BrowserRouter></AuthProvider>}